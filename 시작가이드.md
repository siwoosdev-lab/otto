# 🚀 Claude Code 시작 가이드

> 이 ZIP을 받으셨다면, 5분 안에 OttO 사이트를 띄울 수 있습니다.

---

## 1단계: 압축 풀기 (10초)

```bash
# Mac
unzip otto.zip -d ~/Projects/

# Windows (PowerShell)
Expand-Archive otto.zip -DestinationPath C:\Projects\
```

또는 그냥 더블클릭으로 압축을 풀어 원하는 곳에 `otto/` 폴더를 두세요.

---

## 2단계: Claude Code 설치 (한 번만)

이미 설치되어 있다면 건너뛰세요.

```bash
# 1. Node.js 18+ 필요. 없으면 https://nodejs.org 에서 설치
node --version  # v18.0.0 이상이어야 함

# 2. Claude Code 설치
npm install -g @anthropic-ai/claude-code

# 3. 인증
claude
# → 첫 실행 시 브라우저로 로그인 안내가 나옵니다
```

---

## 3단계: 프로젝트 폴더에서 Claude Code 실행 (10초)

```bash
cd ~/Projects/otto    # 또는 압축 푼 폴더
claude
```

실행되면 Claude Code가 자동으로:
1. `CLAUDE.md` 읽기 (프로젝트 규칙 파악)
2. `README.md` 읽기 (개요 파악)
3. `docs/` 폴더 인식 (필요 시 참조)
4. 디자인 시스템·비용 정책 이해

---

## 4단계: 첫 명령 실행 (1분)

이 중 하나를 그대로 입력해 보세요.

### 옵션 A — 사이트 미리보기

```
> 로컬에서 사이트를 띄워서 확인하고 싶어
```

→ Claude가 알아서 `python3 -m http.server` 또는 `npx serve`를 실행해 줍니다.

### 옵션 B — Vercel 배포

```
> Vercel에 배포해줘
```

→ Claude가 단계별로 안내합니다 (Vercel CLI 설치 → 로그인 → 배포).

### 옵션 C — TODO 확인

```
> TODO.md 보고 다음 작업 추천해줘
```

→ Claude가 우선순위가 가장 높은 작업 3가지를 알려줍니다.

### 옵션 D — Tally 폼 연결

```
> 폼을 Tally에 연결하는 방법 알려줘
```

→ 단계별 가이드 + 코드 변경 자동 적용.

### 옵션 E — 콘텐츠 작성

```
> 소상공인 카페 홍보 게시글 5종 작성해줘
```

→ "도움 요청 톤"으로 5개 버전 작성.

---

## 5단계: 매일 작업 흐름

```bash
# 작업 시작 시
cd ~/Projects/otto
claude

# 작업 후 변경사항 저장 (Git)
git add .
git commit -m "변경 내용"
git push

# Vercel 자동 배포 → 1~2분 후 사이트 반영됨
```

---

## ❓ 자주 묻는 질문

**Q. Claude Code가 처음인데 괜찮나요?**
A. 네. `CLAUDE.md`에 모든 규칙이 적혀있어서 Claude가 알아서 따라갑니다. 처음엔 "TODO 확인해줘", "사이트 띄워줘" 같은 단순한 명령부터 시도해 보세요.

**Q. 잘못된 변경을 했다면?**
A. Git이 있으면 `git checkout .`로 되돌릴 수 있습니다. 처음 받은 ZIP을 보관하면 더 안전합니다.

**Q. 비용은 얼마나 드나요?**
A. Phase 1 운영 비용은 월 약 9,000~14,000원입니다. 상세는 `CLAUDE.md` §4와 §6을 참조하세요.

**Q. 디자인을 마음대로 바꿔도 되나요?**
A. **컬러·폰트·핵심 카피는 변경 금지**입니다. `CLAUDE.md` §3·§9 참조. Claude Code도 이 규칙을 따릅니다.

**Q. 새 기능을 추가하고 싶다면?**
A. 그냥 자연어로 Claude에게 말하세요: "OO 기능 추가해줘". Claude가 규칙 안에서 구현하고, 위반 시 알려줍니다.

---

## 📂 받은 파일 구성

```
otto/
├── CLAUDE.md                    ← Claude Code 작업 규칙 (헌법)
├── README.md                    ← 프로젝트 개요
├── TODO.md                      ← 다음 작업 목록
├── 시작가이드.md                 ← 이 파일
├── .gitignore
├── vercel.json                  ← 배포 설정
├── docs/
│   ├── 01_사업계획서.md
│   ├── 02_사이트_개발계획서.md
│   └── 03_랜딩페이지_설계도.md
└── public/
    ├── index.html              ← 메인 랜딩 페이지
    ├── privacy.html            ← 개인정보처리방침
    ├── terms.html              ← 이용약관
    ├── robots.txt
    └── sitemap.xml
```

---

## 🎯 권장 순서 (오늘 안에 끝낼 수 있는 것)

1. **압축 풀기** (10초)
2. **Claude Code 설치** (5분, 한 번만)
3. **`claude` 실행 + "TODO.md 보고 다음 작업 추천해줘"** (1분)
4. **사이트 로컬 미리보기** (Claude가 알아서, 1분)
5. **Vercel 배포** (Claude가 안내, 5분)
6. **도메인 구매 + 연결** (10분, 가비아 결제 + DNS 설정)

위 6단계를 순서대로 끝내면, 오늘 저녁에 `https://otto.kr`에서 사이트가 실제로 보입니다.

화이팅!
